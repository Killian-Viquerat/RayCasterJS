export class Material {
    constructor(ambiant,diffuse,specular, shininess) {
        this.ambiant = ambiant;
        this.diffuse = diffuse;
        this.specular = specular;
        this.shininess = shininess;
    }
}